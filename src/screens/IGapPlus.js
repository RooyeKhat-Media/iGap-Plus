import React, {Component} from 'react';
import {Text, View} from 'react-native';

class IGapPlus extends Component {
  render() {
    return (
      <View><Text>Hi</Text></View>
    );
  }
}

IGapPlus.propTypes = {
  // myProp: PropTypes.string.isRequired
};

export default IGapPlus;