import Base from '../Base';

/**
 * @property {ProtoInfoTime} _request
 * @property {ProtoInfoTimeResponse} _response
 */
export default class Time extends Base {
  handle() {
    console.error('Time', 'Not implemented yet', this);
  }
}