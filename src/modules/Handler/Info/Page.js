import Base from '../Base';

/**
 * @property {ProtoInfoPage} _request
 * @property {ProtoInfoPageResponse} _response
 */
export default class Page extends Base {
  handle() {
    console.error('Page', 'Not implemented yet', this);
  }
}