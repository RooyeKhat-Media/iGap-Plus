import Base from '../Base';

/**
 * @property {ProtoClientJoinByUsername} _request
 * @property {ProtoClientJoinByUsernameResponse} _response
 */
export default class JoinByUsername extends Base {
  handle() {
    console.error('JoinByUsername', 'Not implemented yet', this);
  }
}