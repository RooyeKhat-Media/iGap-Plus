import Base from '../Base';

/**
 * @property {ProtoClientRoomReport} _request
 * @property {ProtoClientRoomReportResponse} _response
 */
export default class ClientRoomReport extends Base {
  handle() {
  }
}