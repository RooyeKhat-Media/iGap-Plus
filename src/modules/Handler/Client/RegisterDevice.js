import Base from '../Base';

/**
 * @property {ProtoClientRegisterDevice} _request
 * @property {ProtoClientRegisterDeviceResponse} _response
 */
export default class RegisterDevice extends Base {
  handle() {
  }
}