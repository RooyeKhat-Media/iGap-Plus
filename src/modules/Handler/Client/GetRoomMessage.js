import Base from '../Base';

/**
 * @property {ProtoClientGetRoomMessage} _request
 * @property {ProtoClientGetRoomMessageResponse} _response
 */
export default class GetRoomMessage extends Base {
  handle() {
  }
}