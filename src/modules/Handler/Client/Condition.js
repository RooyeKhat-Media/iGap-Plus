import Base from '../Base';

/**
 * @property {ProtoClientCondition} _request
 * @property {ProtoClientConditionResponse} _response
 */
export default class Condition extends Base {
  handle() {
    console.error('Condition', 'Not implemented yet', this);
  }
}