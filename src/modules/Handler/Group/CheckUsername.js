import Base from '../Base';

/**
 * @property {ProtoGroupCheckUsername} _request
 * @property {ProtoGroupCheckUsernameResponse} _response
 */
export default class CheckUsername extends Base {
  handle() {
    console.error('CheckUsername', 'Not implemented yet', this);
  }
}