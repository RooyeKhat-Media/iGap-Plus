import Base from '../../Base';

/**
 * @property {ProtoUserContactsEdit} _request
 * @property {ProtoUserContactsEditResponse} _response
 */
export default class Edit extends Base {
  handle() {
    console.error('Edit', 'Not implemented yet', this);
  }
}