import Base from '../../Base';

/**
 * @property {ProtoUserContactsImport} _request
 * @property {ProtoUserContactsImportResponse} _response
 */
export default class Import extends Base {
  handle() {
    console.error('Import', 'Not implemented yet', this);
  }
}