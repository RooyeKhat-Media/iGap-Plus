import Base from '../../Base';

/**
 * @property {ProtoUserSessionLogout} _request
 * @property {ProtoUserSessionLogoutResponse} _response
 */
export default class Logout extends Base {
  handle() {
    console.error('Logout', 'Not implemented yet', this);
  }
}