import Base from '../Base';

/**
 * @property {ProtoSignalingLeave} _request
 * @property {ProtoSignalingLeaveResponse} _response
 */
export default class Leave extends Base {
  handle() {
    console.error('Leave', 'Not implemented yet', this);
  }
}