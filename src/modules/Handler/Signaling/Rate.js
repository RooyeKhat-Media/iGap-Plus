import Base from '../Base';

/**
 * @property {ProtoSignalingRate} _request
 * @property {ProtoSignalingRateResponse} _response
 */
export default class Rate extends Base {
  handle() {
    console.error('Rate', 'Not implemented yet', this);
  }
}