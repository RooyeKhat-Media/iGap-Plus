import Base from '../Base';

/**
 * @property {ProtoChannelGetDraft} _request
 * @property {ProtoChannelGetDraftResponse} _response
 */
export default class GetDraft extends Base {
  handle() {
    console.error('GetDraft', 'Not implemented yet', this);
  }
}