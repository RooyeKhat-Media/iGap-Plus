import Base from '../Base';

/**
 * @property {ProtoChannelCheckUsername} _request
 * @property {ProtoChannelCheckUsernameResponse} _response
 */
export default class CheckUsername extends Base {
  handle() {
    console.error('CheckUsername', 'Not implemented yet', this);
  }
}