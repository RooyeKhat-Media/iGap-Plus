import Base from '../Base';

/**
 * @property {ProtoChannelUpdateSignature} _request
 * @property {ProtoChannelUpdateSignatureResponse} _response
 */
export default class UpdateSignature extends Base {
  handle() {
    console.error('UpdateSignature', 'Not implemented yet', this);
  }
}