import Base from '../Base';

/**
 * @property {ProtoChatUpdateDraft} _request
 * @property {ProtoChatUpdateDraftResponse} _response
 */
export default class UpdateDraft extends Base {
  handle() {
    console.error('UpdateDraft', 'Not implemented yet', this);
  }
}