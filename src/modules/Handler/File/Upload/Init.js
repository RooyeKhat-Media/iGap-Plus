import Base from '../../Base';

/**
 * @property {ProtoFileUploadInit} _request
 * @property {ProtoFileUploadInitResponse} _response
 */
export default class Init extends Base {
  handle() {
    console.error('Init', 'Not implemented yet', this);
  }
}