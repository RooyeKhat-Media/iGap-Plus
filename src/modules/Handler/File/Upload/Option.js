import Base from '../../Base';

/**
 * @property {ProtoFileUploadOption} _request
 * @property {ProtoFileUploadOptionResponse} _response
 */
export default class Option extends Base {
  handle() {
    console.error('Option', 'Not implemented yet', this);
  }
}