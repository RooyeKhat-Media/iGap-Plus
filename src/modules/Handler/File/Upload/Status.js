import Base from '../../Base';

/**
 * @property {ProtoFileUploadStatus} _request
 * @property {ProtoFileUploadStatusResponse} _response
 */
export default class Status extends Base {
  handle() {
  }
}