import Base from '../Base';

/**
 * @property {ProtoQrCodeNewDevice} _request
 * @property {ProtoQrCodeNewDeviceResponse} _response
 */
export default class NewDevice extends Base {
  handle() {
  }
}