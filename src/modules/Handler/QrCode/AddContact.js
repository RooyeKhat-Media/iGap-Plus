import Base from '../Base';

/**
 * @property {ProtoQrCodeAddContact} _request
 * @property {ProtoQrCodeAddContactResponse} _response
 */
export default class AddContact extends Base {
  handle() {
    console.error('AddContact', 'Not implemented yet', this);
  }
}